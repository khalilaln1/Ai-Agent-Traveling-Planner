# Optional hosted free-model provider.
# The app works without this key, but responses use local rule-based reasoning.
OPENROUTER_API_KEY=
OPENROUTER_FREE_MODEL=liquid/lfm-2.5-1.2b-instruct:free
TRAVEL_AGENT_TIMEOUT=15
